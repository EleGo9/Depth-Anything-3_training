# Learning rate sweep: 10x lower than default.yaml's 5e-6/5e-5 (encoder/head).
# Try this if training loss oscillates or diverges early on.
__inherit__: depth_anything_3.train.configs.default

train:
  lr: 5.0e-7
  save_path: workspace/exp_lr_low
