# bf16 autocast on (lower memory / faster forward, master weights stay fp32). Try this if you're
# memory-constrained -- e.g. it may let you bump train.bs afterwards without OOMing.
__inherit__: depth_anything_3.train.configs.default

train:
  amp_bf16: true
  save_path: workspace/exp_bf16_fast
